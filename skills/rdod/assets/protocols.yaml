# Cross-domain orchestration sequences.
# End-to-end flows spanning multiple domains with step ordering, failure paths, and compensation.
domain_ref: ""

protocols: []
# - name: ""                # e.g., "MultisigRotation"
#   description: ""         # what this end-to-end flow accomplishes
#   participants:
#     - domain: ""          # domain://participant-id
#       role: ""            # what this domain does in the protocol
#   trigger: ""             # what initiates this protocol
#   steps:
#     - seq: 1
#       domain: ""          # domain://participant-id
#       action: ""          # operation name
#       port: ""            # port://domain/inbound/operation (optional)
#       input: ""           # string OR object with description + types
#       # input:
#       #   description: ""
#       #   types:
#       #     - ref: "types://domain-id#TypeName"
#       #       fields: []
#       output: ""          # string OR object with description + type
#       # output:
#       #   description: ""
#       #   type: "types://domain-id#TypeName"
#       preconditions: []   # optional — verification refs
#       # - description: ""
#       #   ref: "verification://domain-id#property-name"
#       on_failure:
#         - error: ""       # ErrorType from errors.yaml
#           ref: ""         # errors://domain-id#ErrorType (optional)
#           action: ""      # escrow | retry | abort | compensate
#           compensation: "" # what to undo if needed
#       notes: ""           # implementation guidance, conditional logic
#       sub_steps: []       # conditional sub-operations within this step
#     - seq: 2
#       domain: ""
#       action: ""
#       depends_on: [1]     # must complete after step 1
#   invariants: []          # guarantees that hold across the entire sequence
#   failure_paths: []       # what happens when each step fails
#   # - step: 1
#   #   failure: ""
#   #   handling: ""
#   timeout: ""             # duration after which the protocol fails
#   compensation: ""        # global rollback if protocol fails after partial completion
#   terminal_states:
#     success: ""           # what 'done' looks like
#     failure: ""           # what 'failed' looks like
