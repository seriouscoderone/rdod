domain_ref: ""

ports:
  # Inbound — driving port: what clients call to use this domain
  - id: ""               # e.g., "port://video-editing/inbound/editing-api"
    type: inbound
    name: ""
    semantics: ""        # command | query | event
    idempotent: false
    contract:
      input: ""          # types://domain-id#InputType or kernel://id#Type
      output: ""         # types://domain-id#OutputType
      errors: []         # - errors://domain-id#ErrorType
    refs: []             # who uses this port, e.g. ["domain://social-media-platform"]

  # Outbound — driven port: what this domain calls out to
  # - id: ""             # e.g., "port://video-editing/outbound/clip-store"
  #   type: outbound
  #   name: ""
  #   semantics: ""      # command | query | event
  #   idempotent: false
  #   contract:
  #     input: ""
  #     output: ""
  #     errors: []
  #   refs: []           # domain:// refs for subdomains; empty for externals
